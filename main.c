#include <stdio.h>
#include <math.h>

int main() {
  float a=2;//modificar por qualquer valor real
  float b=3;//modificar por qualquer valor real
  float c=4;//modificar por qualquer valor real
  float media=a*b*c/3;
  printf("%f\n",media);
  return 0;
}